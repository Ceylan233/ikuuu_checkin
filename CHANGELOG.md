# v1.2.2

- 邮箱验证码默认每 2 秒轮询一次，并在每轮查询前发送 IMAP `NOOP` 刷新邮箱状态，缩短验证码已到达后的发现时间。

# v1.2.1

- 邮箱验证码登录会在提交密码前记录 IMAP UID，只读取本次请求后新到的验证邮件，避免误用旧验证码。
- 登录、验证码、邮箱轮询、签到和账户信息读取均写入即时状态日志，且不再输出登录响应正文。
- LuCI 日志页面每 2 秒自动刷新；手动和定时任务都会写入开始与结束状态。

# v1.2.0

- 账号配置支持 `账号邮箱:iKuuu密码:邮箱密码或授权码`，第三段保持可选。
- 登录返回 `phase=email_code` 时，通过同一会话提交邮件中的 8 位验证码。
- 支持自动识别 QQ/Foxmail、163、126、Gmail、Yahoo、Outlook IMAP，也支持自定义 IMAP。
- OpenWrt LuCI 新增邮箱验证设置并继续以 `600` 权限保存配置。

# v1.1.1

- 适配 iKuuu 新登录流程：使用 HTML 页面请求头并提交 `phase=password`。
- 兼容 `phase=authenticated` 登录成功响应，补齐 AJAX 来源请求头。
- 登录页加载失败时直接报告真实状态，不再继续提交无效请求。

# v1.1.0

- LuCI 新增自定义 iKuuu 域名输入框，支持填写域名或完整 URL。
- 自定义域名优先使用，不可用时继续尝试自动发现与备用域名。

# v1.0.1

- 自定义 SMTP 字段调整到发件邮箱类型选择框正下方。
- 使用标签页分离账号、验证码和邮件设置，避免依赖字段错位。
- 修复空收件人列表导致包装器提前退出的问题。
- 软件包升级后按启用状态自动恢复定时服务。

## v1.0.0

- 支持 163、126、QQ/Foxmail、Gmail、Yahoo、Outlook 和自定义 SMTP。
- 支持一个发件人和多个收件人，自定义 SMTP 可单独填写登录用户名。
- 新增 OpenWrt/iStoreOS LuCI 配置页、每日定时、立即签到、测试邮件和日志。
- 提供可直接使用 `opkg install` 安装的 IPK。
